server:
  port: 3302
# 自动刷新配置
management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"

# 备用配置 当无法从远端拉取配置文件时 使用以下配置
data:
  env: NaN
  user:
    username: NaN
    password: NaN
