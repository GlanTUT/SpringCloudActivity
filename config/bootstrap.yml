spring:
  profiles:
    active: prod
  application:
    name: config-single-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GlanTUT/SpringCloudActivity.git #配置文件所在仓库
          default-label: main   # 配置文件分支 master
          search-paths: config  # 配置文件所在根目录
          skip-ssl-validation: true
          username: 77449339@qq.com
          password: abc520201
          skipSslValidation: true # github的地址是 https:// 开头这里报了一个错误  could not be stablished because of SSL problems

